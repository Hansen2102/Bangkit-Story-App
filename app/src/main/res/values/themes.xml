<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.LoginWithAnimation" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <item name="android:colorBackground">@color/white</item> <!-- Set the background color to white -->
        <item name="android:windowBackground">@color/white</item> <!-- Set the window background color to white -->
        <item name="android:windowActionBarOverlay">true</item> <!-- Optional: Add overlay effect to action bar -->
        <item name="android:windowActionModeOverlay">true</item> <!-- Optional: Add overlay effect to action mode -->
    </style>

    <style name="Theme.LoginWithAnimation" parent="Base.Theme.LoginWithAnimation" />

</resources>